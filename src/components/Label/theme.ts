import { ColorProp, BackgroundProp, MarginProp, PaddingProp } from '../../types'

export interface LabelTheme {
  margin: MarginProp
  padding: PaddingProp
  color: ColorProp
  background: BackgroundProp
}
